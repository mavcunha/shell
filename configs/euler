# vim: ft=sh
# Private repo with Project Euler's solutions
if [[ -d "${HOME}/Projects/euler" ]]; then
  export PROJECT_EULER_DIR=${HOME}/Projects/euler-solutions
  add_to_path "${HOME}/Projects/euler"
fi
