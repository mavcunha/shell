# vim: ft=zsh

# Add current Python 3 to path
add_to_path "/opt/homebrew/opt/python@3.10/bin"

# shortcut for poetry dep management for Python
alias pt='poetry'

# Python fails if is defined as LC_CTYPE=UTF-8
export LC_CTYPE=en_US.UTF-8

